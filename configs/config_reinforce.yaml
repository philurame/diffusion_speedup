name: null
project: "reinforce_cacher"

model_name: "REINFORCE_CACHER"
metric: "patched-lpips"
solver: "DDIM"
scheduler: "LINEAR"
baseline_name: "DEEPCACHE3"

# epochs: 300
epochs: 1
batch_size: 8
max_samples: 64

gs: 5
teacher_nfe: 25
student_nfe: 25
num_steps: 9
num_samples: 8

lr: 0.03
gamma: 0.9999
same_train_noise: True
init_logits: "deepcache-3"